"use client";
import Link from "next/link"; import { useEffect,useState } from "react"; import { getNotifications } from "../data/notification-api";
export function NotificationHeaderLink(){const [count,setCount]=useState(0); useEffect(()=>{let live=true; const load=()=>getNotifications().then(x=>{if(live)setCount(x?.unreadCount??0)}).catch(()=>{}); load(); const id=window.setInterval(load,30000); const focus=()=>load(); window.addEventListener("focus",focus); return()=>{live=false;clearInterval(id);window.removeEventListener("focus",focus)}},[]); return <Link href="/notifications" className="header-action" aria-label={`${count} unread notifications`}><span className="header-action__icon notification-bell" aria-hidden="true">♢{count>0&&<b>{count>99?"99+":count}</b>}</span><span className="header-action__text"><small>Your</small><strong>Updates</strong></span></Link>}
